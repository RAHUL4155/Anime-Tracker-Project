# Data Used By The App

The app keeps three main pieces of data in localStorage:

- `my_anime` for the anime list
- `reminders` for episode reminders
- `settings` for the dark mode setting

Anime search results come from the Jikan API. The search results are not stored until an anime is added to the list.
