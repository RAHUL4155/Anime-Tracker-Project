# Anime Search API

The Discover tab uses the Jikan API to search for anime.

The search request is made when the user enters a search term and clicks Search or presses Enter.

The response is converted to a list of search results. When an anime is added, the app keeps the information it needs such as the title, image, episode count, genres and year.
